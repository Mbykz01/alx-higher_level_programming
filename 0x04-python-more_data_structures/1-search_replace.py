#!/usr/bin/python3
def search_replace(my_list, search, replace):
    another_list = []
    for i in my_list:
        if i == searother_list.append(replace)
        else:i
            another_list.append(i)
    return another_list
